language: rust
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-5
os:
  - linux
  # - osx
rust:
  - stable
  - beta
  - nightly
sudo: false
cache: cargo
before_install: . ./ci/before_install.sh
